<%= form_with(model: computerhacking) do |form| %>
  <% if computerhacking.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(computerhacking.errors.count, "error") %> prohibited this computerhacking from being saved:</h2>

      <ul>
        <% computerhacking.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_area :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
